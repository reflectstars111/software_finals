# 测试说明书 — B 部分（§2.3~§2.5）

> 项目：人格画像雷达系统（Personality Radar） v1.0 | 日期：2026-06-19 | 负责人：B

---

## §2.3 TEST-01：用户认证与权限隔离测试

### 2.3.1 进度安排

| 日期 | 工作内容 | 负责人 |
|------|---------|--------|
| Day1 | 准备测试数据（重置种子用户状态、确认 Docker 服务就绪）| B |
| Day2 | 执行认证与权限全流程测试 | B |
| Day3 | 整理测试结果 | B |

### 2.3.2 条件

- **设备**：个人笔记本电脑（Windows 11），16 GB RAM
- **软件**：Docker Desktop（运行 MySQL + Redis + 后端容器）、Node.js 18+（运行前端）、Chrome/Edge 浏览器（DevTools）、Postman 或 curl
- **人员**：设计：B / 执行：B / 支持：后端负责人
- **其他**：后端服务已正常启动；种子用户就绪：13800000000（Admin@123456，ADMIN）、13900000001（User123456，USER）

### 2.3.3 测试资料

**源码位置**：

| 文件 | 路径 |
|------|------|
| 认证控制器 | `backend/src/main/java/com/personality/radar/controller/AuthController.java` |
| 用户控制器 | `backend/src/main/java/com/personality/radar/controller/UserController.java` |
| 认证服务 | `backend/src/main/java/com/personality/radar/service/AuthService.java` |
| JWT 服务 | `backend/src/main/java/com/personality/radar/security/JwtService.java` |
| JWT 过滤器 | `backend/src/main/java/com/personality/radar/security/JwtAuthenticationFilter.java` |
| 安全配置 | `backend/src/main/java/com/personality/radar/config/SecurityConfig.java` |
| 速率限制 | `backend/src/main/java/com/personality/radar/security/RateLimitFilter.java` |
| 异常处理 | `backend/src/main/java/com/personality/radar/common/GlobalExceptionHandler.java` |
| 前端登录页 | `frontend/src/views/LoginView.vue` |
| 前端路由 | `frontend/src/router.ts` |

**测试用例表**：

| 用例编号 | 测试场景 | 输入 | 预期结果 |
|---------|---------|------|---------|
| T01-01 | 正常注册 | 网页：注册页输入手机号 13900000003 + 密码 User123456 + 昵称"测试用户"；API：POST /api/auth/register | 注册成功，跳转首页；API 返回 code=0，含 token 和 user 对象，role=USER |
| T01-02 | 重复手机号注册 | 网页：再次使用 13900000001 注册；API：POST /api/auth/register | 页面提示"手机号已注册"；API 返回 code=409 |
| T01-03 | 并发注册同一手机号 | 使用 curl 并发发送两个 POST /api/auth/register（同一新手机号） | 一个返回 code=0，另一个返回 code=409（DataIntegrityViolationException 处理） |
| T01-04 | 正常登录 | 网页：登录页输入 13900000001 / User123456；API：POST /api/auth/login | 登录成功，跳转首页；API 返回 code=0，含 JWT token + user 对象 |
| T01-05 | 错误密码登录 | 网页/API：输入正确手机号 + 错误密码 | 提示"账号或密码错误"；API 返回 code=401 |
| T01-06 | 不存在的用户登录 | 网页/API：输入 13999999999 / 任意密码 | 提示"账号或密码错误"；API 返回 code=401（不暴露用户是否存在） |
| T01-07 | 登录锁定机制 | 连续 5 次使用错误密码登录同一账号；第 6 次使用正确密码 | 前 5 次：code=401；第 6 次：code=429 "登录失败次数过多，请稍后再试" |
| T01-08 | 锁定后正确密码登录 | 等待 10 分钟，使用正确密码登录 | 登录成功，code=0，lockedUntil 清空 |
| T01-09 | 已停用用户登录 | 管理员将 13900000002 停用后，该用户尝试登录 | 提示"账号已停用"；API 返回 code=403 |
| T01-10 | Token 访问受保护接口 | 不带 Authorization 头访问 GET /api/users/me | API 返回 HTTP 401 "请先登录" |
| T01-11 | Token 访问管理员接口 | 普通用户（13900000001）的 Token 访问 GET /api/admin/stats | API 返回 HTTP 403 "无权访问该功能" |
| T01-12 | 管理员访问管理接口 | 管理员（13800000000）登录后访问 GET /api/admin/stats | API 返回 code=0，含统计数据 |
| T01-13 | 过期 Token 访问 | 使用过期 JWT 访问受保护接口 | API 返回 HTTP 401 |
| T01-14 | 速率限制 | 1 分钟内连续发送 11 次 POST /api/auth/login | 第 11 次返回 HTTP 429 "请求过于频繁，请稍后再试" |
| T01-15 | 修改个人信息 | PUT /api/users/me，body: {"displayName":"新昵称","avatarUrl":"https://..."} | 返回 code=0，displayName 更新为新昵称 |
| T01-16 | 无效输入注册 | POST /api/auth/register，phone 字段缺少或格式错误 | API 返回 HTTP 400 "输入内容格式不正确" |
| T01-17 | 空密码注册 | POST /api/auth/register，密码为空 | API 返回 HTTP 400 |
| T01-18 | 前端路由守卫 | 未登录状态访问 /tests/personality | 页面跳转至 /login |

**输入输出样例**：

| 样例 | 说明 | 输入 | 预期输出 |
|------|------|------|---------|
| S01-01 | 正常注册 | POST /api/auth/register，body: {"phone":"13900000003","password":"User123456","displayName":"测试用户"} | {"code":0,"data":{"token":"eyJ...","user":{"id":4,"phone":"13900000003","displayName":"测试用户","role":"USER","active":true}}} |
| S01-02 | 重复注册 | POST /api/auth/register，body: {"phone":"13900000001",...} | {"code":409,"message":"手机号已注册","data":null} |
| S01-03 | 登录成功 | POST /api/auth/login，body: {"phone":"13900000001","password":"User123456"} | {"code":0,"data":{"token":"eyJ...","user":{"id":2,"phone":"13900000001","displayName":"晨间探索者","role":"USER","active":true}}} |
| S01-04 | 密码错误 | POST /api/auth/login，body: {"phone":"13900000001","password":"wrong"} | {"code":401,"message":"账号或密码错误","data":null} |
| S01-05 | 登录锁定 | 第 6 次连续失败 | {"code":429,"message":"登录失败次数过多，请稍后再试","data":null} |
| S01-06 | 账号停用 | 被停用用户登录 | {"code":403,"message":"账号已停用","data":null} |
| S01-07 | 普通用户访问Admin | GET /api/admin/stats，Authorization: Bearer <user_token> | {"code":403,"message":"无权访问该功能","data":null} |
| S01-08 | 未登录访问 | GET /api/users/me，无 Authorization | {"code":401,"message":"请先登录","data":null} |
| S01-09 | 速率限制 | 第 11 次请求 | HTTP 429 + {"code":429,"message":"请求过于频繁，请稍后再试","data":null} |

### 2.3.4 测试设计

**a) 控制方式**：半自动测试。API 调用使用 curl 发送，前端页面人工验证 UI 展示和交互。结果记录：curl 响应截图 + 浏览器前端截图 + 文字记录。

**b) 输入数据选择说明**：

| 数据类别 | 选择理由 |
|---------|---------|
| 种子用户 13900000001（USER）| 验证正常用户注册/登录/鉴权流程 |
| 种子用户 13800000000（ADMIN）| 验证管理员角色鉴权 |
| 新手机号 13900000003 | 验证正常注册流程 |
| 已存在手机号 13900000001 | 验证重复注册拦截 |
| 并发双请求同一手机号 | 验证 TOCTOU 竞态处理 |
| 连续 5 次错误密码 | 验证账户锁定机制 |
| 无 Token / 过期 Token / 用户 Token 访问 Admin | 验证 JWT 鉴权和角色控制 |
| 1 分钟内 11 次登录请求 | 验证速率限制 |

**c) 预期输出**：
- 注册成功返回 JWT token（HMAC-SHA384 签名），24h 有效期
- 重复注册返回 code=409（DataIntegrityViolationException 转换）
- 并发注册竞态：一个成功（code=0），一个返回 409
- 密码连续错误 5 次后锁定 10 分钟，返回 code=429
- 所有失败响应 code≠0，含中文提示，不暴露内部异常
- 受保护接口无 Token 返回 HTTP 401，无权限返回 HTTP 403
- 管理接口仅 ADMIN 角色可访问
- 速率限制：每 IP 每分钟 10 次（auth/share/admin 端点），超出返回 429

**d) 测试过程**：

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1. 准备 | 确认 Docker 服务就绪，种子用户状态正常 | 后端健康检查 UP |
| 2. 执行 T01-01 | curl POST /api/auth/register 新手机号 | code=0，返回 token |
| 3. 执行 T01-02 | curl POST /api/auth/register 已有手机号 | code=409 "手机号已注册" |
| 4. 执行 T01-03 | 并发 curl 发送两个相同手机号的注册请求 | 一个 0 一个 409 |
| 5. 执行 T01-04 | curl POST /api/auth/login 正确凭据 | code=0，返回 token |
| 6. 执行 T01-05 | curl POST /api/auth/login 错误密码 | code=401 |
| 7. 执行 T01-07 | 连续 5 次错误密码 | 第 6 次 code=429 |
| 8. 执行 T01-09 | 管理员停用用户，该用户尝试登录 | code=403 |
| 9. 执行 T01-10 | 不带 Token 访问 GET /api/users/me | HTTP 401 |
| 10. 执行 T01-11 | 普通用户 Token 访问 GET /api/admin/stats | HTTP 403 |
| 11. 执行 T01-12 | 管理员 Token 访问 GET /api/admin/stats | code=0 |
| 12. 执行 T01-14 | 1 分钟内 11 次 POST /api/auth/login | 第 11 次 HTTP 429 |
| 13. 执行 T01-18 | 浏览器未登录访问 /tests/personality | 跳转 /login |
| 14. 收尾 | 截图归档，填写测试结果表 | 所有用例记录完整 |

---

## §2.4 TEST-02：性格测试答题与得分计算测试

### 2.4.1 进度安排

| 日期 | 工作内容 | 负责人 |
|------|---------|--------|
| Day1 | 准备测试数据（确认题库就绪、种子用户可答题）| B |
| Day2 | 执行答题与得分验证测试 | B |
| Day3 | 整理测试结果 | B |

### 2.4.2 条件

- **设备**：个人笔记本电脑（Windows 11），16 GB RAM
- **软件**：Docker Desktop、Node.js 18+、Chrome/Edge DevTools、Postman 或 curl
- **人员**：设计：B / 执行：B / 支持：后端负责人
- **其他**：后端服务正常启动；44 道种子题目已加载（14 性格 + 10 饮食 + 10 旅游 + 10 社交）；种子用户 13900000001 已激活

### 2.4.3 测试资料

**源码位置**：

| 文件 | 路径 |
|------|------|
| 题目控制器 | `backend/src/main/java/com/personality/radar/controller/QuestionController.java` |
| 测试控制器 | `backend/src/main/java/com/personality/radar/controller/TestController.java` |
| 测试服务 | `backend/src/main/java/com/personality/radar/service/TestService.java` |
| 评分引擎 | `backend/src/main/java/com/personality/radar/service/ScoringEngine.java` |
| 题目实体 | `backend/src/main/java/com/personality/radar/domain/Question.java` |
| 选项实体 | `backend/src/main/java/com/personality/radar/domain/QuestionOption.java` |
| 测试结果实体 | `backend/src/main/java/com/personality/radar/domain/TestResult.java` |
| 维度枚举 | `backend/src/main/java/com/personality/radar/domain/PersonalityDimension.java` |
| 前端测试页 | `frontend/src/views/TestView.vue` |
| 前端题库加载 | `frontend/src/services/testService.ts` |

**测试用例表**：

| 用例编号 | 测试场景 | 输入 | 预期结果 |
|---------|---------|------|---------|
| T02-01 | 获取性格测试题目 | API：GET /api/questions?type=personality | 返回 14 道题目，每题含 5 个选项（李克特量表 1-5），每个选项含 10 维权重 |
| T02-02 | 获取饮食测试题目 | API：GET /api/questions?type=food | 返回 10 道题目，包含 FOOD_ADVENTURE 和 FOOD_SOCIAL 维度权重 |
| T02-03 | 获取旅游测试题目 | API：GET /api/questions?type=travel | 返回 10 道题目，包含 TRAVEL_ADVENTURE 和 TRAVEL_PLANNING 维度权重 |
| T02-04 | 获取社交测试题目 | API：GET /api/questions?type=social | 返回 10 道题目，包含 SOCIAL_ENERGY 维度权重 |
| T02-05 | 无效测试类型 | API：GET /api/questions?type=invalid | 返回 HTTP 400 "测试类型不存在" |
| T02-06 | 正常提交答案 | API：POST /api/tests/submit，提交性格测试 14 题答案（全部选"非常同意"=5） | code=0，返回 TestResult，scores 含 10 个维度分数 |
| T02-07 | 提交答案后验证分数范围 | 检查 T02-06 返回的维度分数 | 所有 10 个维度分数 ∈ [0, 100]，personality 维度不全是 50 |
| T02-08 | 不同答案产生不同分数 | 提交全部选"非常不同意"=1 的答案，与 T02-06 对比 | 分数明显不同（开放/尽责/外向/宜人维度有显著差异） |
| T02-09 | 逆向题计分正确 | 检查题目"经常临时决定重要安排"(reverse=true)计分 | 选 5→1 分、选 1→5 分（逆向映射） |
| T02-10 | 生活方式维度计分 | 提交饮食测试全部选"非常同意" | FOOD_ADVENTURE 和 FOOD_SOCIAL 维度分数 > 50 |
| T02-11 | 缺失维度默认50 | 仅提交性格测试，检查 TRAVEL_ADVENTURE | TRAVEL_ADVENTURE = 50（未被赋权的维度默认中性） |
| T02-12 | 题目与选项一致性 | 检查返回选项的 questionId 是否与题目匹配 | 所有选项的 question 字段指向正确题目 |
| T02-13 | 题目不存在的ID | API：POST /api/tests/submit，answers 含不存在的 questionId | 返回 HTTP 400 "题目不存在" |
| T02-14 | 选项不属于题目 | API：POST /api/tests/submit，answers 含不属于该题的 optionId | 返回 HTTP 400 "选项不属于当前题目" |
| T02-15 | 查看测试历史 | API：GET /api/tests/history | 返回 TestResult 数组，包含之前提交的测试记录 |
| T02-16 | 前端答题流程 | 浏览器访问 /tests/personality，完成全部题目后点击"生成报告" | 进度条正确更新，提交后跳转 /report |
| T02-17 | 前端防漏答 | 浏览器答题页，未完成所有题目点击"生成报告" | 提示"你还有 X 道题未完成" |
| T02-18 | 前端 loading 状态 | 浏览器访问 /tests，等待题目加载 | 显示"正在加载题库..." |

**输入输出样例**：

| 样例 | 说明 | 输入 | 预期输出 |
|------|------|------|---------|
| S02-01 | 提交答案 | POST /api/tests/submit，body: {"type":"personality","answers":[{"questionId":1,"optionIds":[5]},{"questionId":2,"optionIds":[12]},...14条]} | {"code":0,"data":{"id":21,"type":"personality","scores":{"OPENNESS":61,"CONSCIENTIOUSNESS":61,"EXTRAVERSION":68,"AGREEABLENESS":61,"NEUROTICISM":50,"FOOD_ADVENTURE":50,"FOOD_SOCIAL":50,"TRAVEL_ADVENTURE":50,"TRAVEL_PLANNING":50,"SOCIAL_ENERGY":50},"createdAt":"2026-06-19T..."}} |
| S02-02 | 答案极端差异 | POST /api/tests/submit，全部选 1（非常不同意） | scores 中各人格维度 < 50（如 OPENNESS=39, EXTRAVERSION=32） |
| S02-03 | 饮食测试得分 | POST /api/tests/submit 饮食测试全部选 5 | FOOD_ADVENTURE=85, FOOD_SOCIAL=55 |
| S02-04 | 题目不存在 | POST /api/tests/submit，answers: [{"questionId":9999,"optionIds":[1]}] | {"code":400,"message":"题目不存在","data":null} |
| S02-05 | 查看历史 | GET /api/tests/history | {"code":0,"data":[{"id":...,"type":"personality","scores":{...},"createdAt":"..."},...]} |

### 2.4.4 测试设计

**a) 控制方式**：半自动测试。API 调用使用 curl 发送验证数据正确性，前端页面人工验证 UI 展示和交互。

**b) 输入数据选择说明**：

| 数据类别 | 选择理由 |
|---------|---------|
| 全部选 5（非常同意）| 验证理想情况下的最高分计算 |
| 全部选 1（非常不同意）| 验证极端低分的正确性，与全 5 对比验证分数差异化 |
| 不完整答案（12/14 题）| 验证缺失维度默认 50 |
| 无效题目 ID | 验证输入校验和错误提示 |
| 饮食/旅游/社交测试 | 验证生活方式维度权重映射 |

**c) 预期输出**：
- 题目返回结构：含 id, type, content, options[]（每题 5 个选项，每个选项含 10 维权重）
- 所有维度分数 ∈ [0, 100]，计算公式：score = round((avg_weight - 1) / 4 × 100)
- 未被题目覆盖的维度默认为 50
- 逆向题选 5→1 分，选 1→5 分
- 全部选 5 的 personality 维度分数应 > 50，全部选 1 的分数应 < 50
- 饮食测试的 FOOD_ADVENTURE 和 FOOD_SOCIAL 分数应有明显变化
- 所有失败响应 code≠0，含中文提示

**d) 测试过程**：

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1. 准备 | 登录 13900000001，确认题库可访问 | 四类题目各返回正确数量 |
| 2. 执行 T02-01~04 | curl GET /api/questions?type=personality/food/travel/social | 各返回 14/10/10/10 题，每题 5 选项 |
| 3. 执行 T02-06 | curl POST /api/tests/submit 全部选 5 | code=0，10 个维度分数，personality 维度 ≠50 |
| 4. 执行 T02-08 | curl POST /api/tests/submit 全部选 1 | 与前一步分数显著不同 |
| 5. 执行 T02-10 | curl POST /api/tests/submit 饮食测试全选 5 | FOOD_ADVENTURE >50 |
| 6. 执行 T02-13 | curl POST /api/tests/submit 错误题目ID | code=400 |
| 7. 执行 T02-15 | curl GET /api/tests/history | 返回历史列表 |
| 8. 执行 T02-16 | 浏览器访问 /tests/personality 完整答题 | 进度条更新，提交后跳转 /report |
| 9. 执行 T02-17 | 浏览器跳过题目直接提交 | 提示未完成 |
| 10. 收尾 | 截图归档 | 所有用例记录完整 |

---

## §2.5 TEST-03：画像报告与雷达图渲染测试

### 2.5.1 进度安排

| 日期 | 工作内容 | 负责人 |
|------|---------|--------|
| Day1 | 准备测试数据（确保用户已完成四类测试）| B |
| Day2 | 执行报告与渲染测试 | B |
| Day3 | 整理测试结果 | B |

### 2.5.2 条件

- **设备**：个人笔记本电脑（Windows 11），16 GB RAM
- **软件**：Docker Desktop、Node.js 18+、Chrome/Edge DevTools（含移动端模拟）、Postman 或 curl
- **人员**：设计：B / 执行：B / 支持：后端负责人
- **其他**：后端服务正常启动；用户 13900000001 已提交全部四类测试；前端依赖已安装（echarts 5.x）

### 2.5.3 测试资料

**源码位置**：

| 文件 | 路径 |
|------|------|
| 报告控制器 | `backend/src/main/java/com/personality/radar/controller/ReportController.java` |
| 报告服务 | `backend/src/main/java/com/personality/radar/service/ReportService.java` |
| 报告快照实体 | `backend/src/main/java/com/personality/radar/domain/ReportSnapshot.java` |
| 前端报告页 | `frontend/src/views/ReportView.vue` |
| 雷达图组合函数 | `frontend/src/composables/useRadarChart.ts` |
| 维度显示工具 | `frontend/src/utils/dimensions.ts` |
| 报告服务（前端）| `frontend/src/services/reportService.ts` |

**测试用例表**：

| 用例编号 | 测试场景 | 输入 | 预期结果 |
|---------|---------|------|---------|
| T03-01 | 获取综合画像报告 | API：GET /api/reports/me | code=0，返回含 user/scores/indicators/radarValues/interpretations/suggestions 的完整报告 |
| T03-02 | 报告含10个维度 | 检查 T03-01 返回的 scores 字段 | 包含全部 10 个维度键：OPENNESS, CONSCIENTIOUSNESS, EXTRAVERSION, AGREEABLENESS, NEUROTICISM, FOOD_ADVENTURE, FOOD_SOCIAL, TRAVEL_ADVENTURE, TRAVEL_PLANNING, SOCIAL_ENERGY |
| T03-03 | 报告聚合四类测试 | 分别提交不同分值的四类测试，检查报告分数 | 报告取各维度最高分（如 personality OPENNESS=61, food OPENNESS=70 → 报告 OPENNESS=70） |
| T03-04 | NEUROTICISM 反转为情绪稳定性 | 检查报告 indicators 中 NEUROTICISM 的标签和值 | 标签为"情绪稳定性"，radarValues 中该值为 100 - 原始NEUROTICISM分 |
| T03-05 | 维度解释文字 | 检查 report.interpretations 数组 | 10 个中文解释字符串，每个含维度名 + 分数 + 个性化解说 |
| T03-06 | 个性化建议 | 检查 report.suggestions 数组 | 含通用建议和针对最高维度生成的优先建议 |
| T03-07 | 报告快照历史 | API：GET /api/reports/history | 返回 ReportSnapshot 数组，按时间降序 |
| T03-08 | 获取指定快照 | API：GET /api/reports/{id} | 返回指定快照的完整报告 |
| T03-09 | 无测试数据 | 使用无测试记录的账号调用 GET /api/reports/me | 返回 code=400 "请先完成基础性格测试" |
| T03-10 | 他人报告不可见 | 用户A 尝试 GET /api/reports/{用户B的reportId} | 返回 code=404 "报告不存在"（数据隔离） |
| T03-11 | 前端雷达图渲染 | 浏览器访问 /report（用户已完成测试） | 页面展示 10 维度雷达图（ECharts），图表不溢出、不截断、无控制台报错 |
| T03-12 | 前端雷达图自适应 | 浏览器窗口缩放至移动端宽度（375px）| 雷达图宽度自适应，不变形、不超出视口 |
| T03-13 | 前端 loading 状态 | 浏览器首次访问 /report | 显示"正在生成报告..."加载动画 |
| T03-14 | 前端空状态 | 无测试数据的用户浏览器访问 /report | 显示"你还没有完成测试" + "去完成测试"按钮 |
| T03-15 | 前端错误状态 | 模拟后端返回 500 | 显示错误提示信息 |
| T03-16 | Demo 模式 | 浏览器访问 /report?demo=true | 不依赖后端数据，展示示例报告 |
| T03-17 | 移动端布局 | Chrome DevTools 模拟 iPhone 12 (390×844) 访问 /report | 卡片纵向排列、按钮 >=44px、雷达图不溢出、文字可读 |

**输入输出样例**：

| 样例 | 说明 | 输入 | 预期输出（关键字段） |
|------|------|------|--------------------|
| S03-01 | 正常报告 | GET /api/reports/me | {"code":0,"data":{"user":{...},"scores":{"OPENNESS":82,...10维},"indicators":[{"name":"开放性","max":100},...10个],"radarValues":[82,66,74,68,62,75,80,72,54,78],"interpretations":["开放性：82 分，你更容易被新体验、创意和未知路线吸引...",...10条],"suggestions":["..."]}} |
| S03-02 | 无测试数据 | GET /api/reports/me（无测试用户） | {"code":400,"message":"请先完成基础性格测试","data":null} |
| S03-03 | 报告历史 | GET /api/reports/history | {"code":0,"data":[{"id":1,"summary":"开放性：82 分...","createdAt":"2026-06-19T..."}]} |

### 2.5.4 测试设计

**a) 控制方式**：半自动测试。API 使用 curl 验证数据结构正确性，前端使用 Chrome DevTools 验证渲染和响应式。

**b) 输入数据选择说明**：

| 数据类别 | 选择理由 |
|---------|---------|
| 已完成四类测试的用户（13900000001）| 验证完整报告生成（10 维聚合），含 lifestyle 维度 |
| 仅完成性格测试的用户 | 验证 lifestyle 维度默认为 50 |
| 无任何测试的用户 | 验证错误提示和前端空状态 |
| 手机端分辨率（375-390px）| 验证响应式设计和雷达图自适应 |
| /report?demo=true | 验证 demo 模式不依赖后端 |

**c) 预期输出**：
- 报告 scores 含 10 个维度键值对
- indicators 含 10 个指标（name + max=100）
- radarValues 含 10 个数值，NEUROTICISM 反转显示
- interpretations 含 10 个中文字符串（每个维度含分数 + 个性化解说）
- suggestions 含通用建议 + 针对最高维度的优先建议
- 前端 ECharts 雷达图正常渲染，自适应窗口
- 移动端布局完整，无横向溢出
- 所有状态覆盖：loading / empty / error / success

**d) 测试过程**：

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1. 准备 | 以 13900000001 登录，确认已完成四类测试（提交各类型测试）| 历史记录含 4 条 |
| 2. 执行 T03-01 | curl GET /api/reports/me | code=0，10 维完整报告 |
| 3. 执行 T03-03 | 分别检查 personality 和 food 的相同维度最高分 | 报告取 max |
| 4. 执行 T03-04 | 检查 NEUROTICISM 反转 | indicators[4].name="情绪稳定性" |
| 5. 执行 T03-08 | curl GET /api/reports/history | 快照历史 |
| 6. 执行 T03-11 | 浏览器访问 /report | 雷达图正常渲染，无控制台错误 |
| 7. 执行 T03-12 | Chrome DevTools 切换到 375px 移动端 | 雷达图自适应 |
| 8. 执行 T03-14 | 无测试账号访问 /report | 显示空状态 |
| 9. 执行 T03-16 | 浏览器访问 /report?demo=true | 显示示例报告 |
| 10. 收尾 | 截图归档 | 所有用例记录完整 |

---

---

## §2.6 TEST-04：场景推荐与反馈闭环测试

### 2.6.1 进度安排

| 日期 | 工作内容 | 负责人 |
|------|---------|--------|
| Day2 | 准备测试数据（确认用户已完成基础性格测试、种子推荐项就绪）| B |
| Day3 | 执行推荐加载 + 反馈闭环测试 | B |
| Day4 | 整理测试结果 | B |

### 2.6.2 条件

- **设备**：个人笔记本电脑（Windows 11），16 GB RAM
- **软件**：Docker Desktop（运行 MySQL + Redis + 后端容器）、Node.js 18+（运行前端）、Chrome/Edge 浏览器（DevTools）、Postman 或 curl
- **人员**：设计：B / 执行：B / 支持：后端负责人
- **其他**：后端服务正常启动；150 个种子推荐项就绪（5 场景 × 30 项）；用户 13900000001 已完成基础性格测试；4 个推荐规则（explore/structured/social/gentle）已启用

### 2.6.3 测试资料

**源码位置**：

| 文件 | 路径 |
|------|------|
| 推荐控制器 | `backend/src/main/java/com/personality/radar/controller/RecommendationController.java` |
| 推荐服务 | `backend/src/main/java/com/personality/radar/service/RecommendationService.java` |
| 推荐排序引擎 | `backend/src/main/java/com/personality/radar/service/RecommendationRanker.java` |
| 推荐项实体 | `backend/src/main/java/com/personality/radar/domain/RecommendationItem.java` |
| 推荐规则实体 | `backend/src/main/java/com/personality/radar/domain/RecommendationRule.java` |
| 反馈实体 | `backend/src/main/java/com/personality/radar/domain/Feedback.java` |
| 用户偏好实体 | `backend/src/main/java/com/personality/radar/domain/UserPreference.java` |
| 前端推荐页 | `frontend/src/views/RecommendationsView.vue` |
| 前端推荐服务 | `frontend/src/services/recommendationService.ts` |

**测试用例表**：

| 用例编号 | 测试场景 | 输入 | 预期结果 |
|---------|---------|------|---------|
| T04-01 | 获取餐饮推荐 | API：GET /api/recommendations?scene=food | code=0，返回 ≤10 项推荐，每项含 id/title/description/tags/score/baseScore |
| T04-02 | 获取旅游推荐 | API：GET /api/recommendations?scene=travel | code=0，≤10 项，按 score 降序排列 |
| T04-03 | 获取社交推荐 | API：GET /api/recommendations?scene=social | code=0，≤10 项 |
| T04-04 | 获取穿搭推荐 | API：GET /api/recommendations?scene=outfit | code=0，≤10 项 |
| T04-05 | 获取生涯推荐 | API：GET /api/recommendations?scene=career | code=0，≤10 项 |
| T04-06 | 无效场景类型 | API：GET /api/recommendations?scene=invalid | HTTP 400 "推荐场景不存在" |
| T04-07 | 无性格测试 | 无 PERSONALITY 测试记录的用户调用推荐接口 | code=400 "请先完成基础性格测试" |
| T04-08 | 推荐得分范围 | 检查返回的 score 字段 | 所有 score ∈ [0, 100] |
| T04-09 | 推荐排序验证 | 检查返回列表的 score 字段是否降序 | score[i] ≥ score[i+1]（降序排列） |
| T04-10 | 得分差异化 | 比较同一场景不同用户的推荐排序 | 不同用户的推荐排序应不同（因性格画像不同） |
| T04-11 | 提交"喜欢"反馈 | API：POST /api/recommendations/{id}/feedback，body: {"rating":"LIKE"} | code=0；该推荐项标签偏好权重 +3 |
| T04-12 | 提交"不喜欢"反馈 | API：POST /api/recommendations/{id}/feedback，body: {"rating":"DISLIKE"} | code=0；该推荐项标签偏好权重 -3 |
| T04-13 | 提交"一般"反馈 | API：POST /api/recommendations/{id}/feedback，body: {"rating":"NEUTRAL"} | code=0；该推荐项标签偏好权重 +1 |
| T04-14 | 无效评价类型 | API：POST feedback，body: {"rating":"INVALID"} | HTTP 400 "评价类型不正确" |
| T04-15 | 不存在的推荐项 | API：POST /api/recommendations/9999/feedback | code=404 "推荐项不存在" |
| T04-16 | 反馈后排序变化 | 提交 DISLIKE 反馈 → 重新加载推荐列表 | 同标签推荐项得分降低，排序下移；无该标签的推荐项相对排名上升 |
| T04-17 | 查看反馈历史 | API：GET /api/recommendations/feedback/me | code=0，返回 UserFeedback 数组，含 itemTitle/scene/rating/createdAt |
| T04-18 | 前端 Tab 切换 | 浏览器依次点击"餐饮/旅行/社交/穿搭/职业"标签 | 每个标签切换时显示 LoadingState → 对应场景推荐渲染 |
| T04-19 | 前端反馈交互 | 浏览器推荐页点击"喜欢/一般/不喜欢"→ 观察页面变化 | 显示反馈成功提示；推荐列表重新排序 |
| T04-20 | 前端推荐卡片内容 | 检查推荐卡片渲染内容 | 卡片含：场景标签/标题/描述/标签行/匹配度百分比/反馈按钮 |

**输入输出样例**：

| 样例 | 说明 | 输入 | 预期输出 |
|------|------|------|---------|
| S04-01 | 餐饮推荐 | GET /api/recommendations?scene=food | `{"code":0,"data":[{"id":...,"scene":"food","title":"创意融合小馆","description":"...","tags":["explore","social"],"score":76,"baseScore":68},...10项]}` |
| S04-02 | 无效场景 | GET /api/recommendations?scene=invalid | `{"code":400,"message":"推荐场景不存在","data":null}` |
| S04-03 | 无测试 | GET /api/recommendations?scene=food（无测试用户）| `{"code":400,"message":"请先完成基础性格测试","data":null}` |
| S04-04 | 喜欢反馈 | POST /api/recommendations/1/feedback，body：`{"rating":"LIKE"}` | `{"code":0,"message":"success","data":null}` |
| S04-05 | 反馈后重排 | POST DISLIKE → GET /api/recommendations?scene=food | 同标签推荐降序，无该标签推荐相对上升 |
| S04-06 | 反馈历史 | GET /api/recommendations/feedback/me | `{"code":0,"data":[{"id":...,"itemTitle":"创意融合小馆","scene":"FOOD","rating":"LIKE","createdAt":"..."}]}` |

### 2.6.4 测试设计

**a) 控制方式**：半自动测试。API 使用 curl 验证算法正确性（得分范围、排序、反馈闭环），前端使用 Chrome DevTools 验证 UI 渲染和交互反馈。

**b) 输入数据选择说明**：

| 数据类别 | 选择理由 |
|---------|---------|
| 5 个有效场景（food/travel/social/outfit/career）| 验证全部场景推荐可用 |
| 无效场景（invalid）| 验证枚举校验 |
| 无测试记录的用户 | 验证前置条件检查（400 错误） |
| LIKE/NEUTRAL/DISLIKE 三种反馈 | 验证反馈闭环（权重增量 +3/+1/-3） |
| 不存在的推荐项 ID（9999）| 验证 404 处理 |
| 用户 13900000001 的高社交能量画像 | 验证 social 标签推荐项排序靠前 |

**c) 预期输出**：
- 推荐排序算法公式：

  ```
  score = profileFit × 0.68       // 性格匹配（主信号，含偏差放大 ×1.6）
        + baseScore × 0.22        // 基础品质
        + preferenceDelta         // 反馈微调（tanh(pref/18) × 10，限幅 ±10）
        + ruleDelta               // 规则微调（weight × 1.2，限幅 ±6）
  ```

- 性格匹配按标签加权平均：`explore`(OPENNESS×0.45+FOOD_ADVENTURE×0.25+TRAVEL_ADVENTURE×0.30) / `structured`(CONSCIENTIOUSNESS×0.65+TRAVEL_PLANNING×0.25+EMOTIONAL_STABILITY×0.10) / `social`(EXTRAVERSION×0.50+SOCIAL_ENERGY×0.30+FOOD_SOCIAL×0.15+AGREEABLENESS×0.05) / `gentle`(NEUROTICISM×0.45+AGREEABLENESS×0.25+INTROVERSION×0.20+TRAVEL_PLANNING×0.10)
- 每场景返回 ≤10 项，按 score 降序
- 反馈不影响当次排序，下次加载时生效
- 偏好权重范围 [-30, 30]，使用 tanh 函数确保微调有界
- 所有失败响应 code≠0，含中文提示

**d) 测试过程**：

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1. 准备 | 以 13900000001 登录，确认已完成 PERSONALITY 测试 | 报告可正常查看 |
| 2. 执行 T04-01~05 | curl GET 5 个场景推荐 | 各返回 ≤10 项，score ∈ [0,100]，降序 |
| 3. 执行 T04-06 | curl GET scene=invalid | code=400 |
| 4. 执行 T04-10 | 用不同画像用户分别调推荐接口 | 排序不同 |
| 5. 执行 T04-11 | curl POST LIKE 反馈 | code=0 |
| 6. 执行 T04-16 | 连续 DISLIKE 某推荐项 5 次 → 重新加载 | 该标签推荐排序下降 |
| 7. 执行 T04-14 | curl POST rating=INVALID | code=400 |
| 8. 执行 T04-15 | curl POST 不存在的推荐项 ID | code=404 |
| 9. 执行 T04-17 | curl GET /api/recommendations/feedback/me | 返回反馈历史 |
| 10. 执行 T04-18 | 浏览器依次点击 5 个 Tab | Loading → 渲染切换正常 |
| 11. 执行 T04-19 | 浏览器点击"喜欢"→ 观察 | 提示 + 重排 |
| 12. 执行 T04-20 | 检查卡片渲染内容 | 含标题/标签/得分/按钮 |
| 13. 收尾 | 截图归档 | 所有用例记录完整 |

---

**文档版本**：v0.1 | **编写日期**：2026-06-19 | **编写人**：B
